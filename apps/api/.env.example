# Required — PostgreSQL connection string (loaded by @careos/db at import time)
DATABASE_URL=postgresql://postgres:careos@localhost:5432/careos

# Optional — defaults to 3000
# PORT=3000

# Required — Anthropic API key (used by @careos/ai for chart note filling)
ANTHROPIC_API_KEY=
